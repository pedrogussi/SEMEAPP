<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nemaacmk3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_semeapp2"
        android:label="@string/SEMEAPP"
        android:roundIcon="@mipmap/logo_semeapp2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".calcularDist" />
        <activity android:name=".pgmMk1" />
        <activity android:name=".marcadoresLinhas" />
        <activity android:name=".dicas1" />
        <activity android:name=".mk4" />
        <activity android:name=".calcular1" />
        <activity android:name=".regulagem1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>